namespace LambdaFlow {

    internal enum Platform {
        WINDOWS,
        LINUX,
        MACOS,
        ANDROID,
        IOS,
        UNKNOWN
    }

    internal enum SecurityMode {
        MINIMAL,
        INTEGRITY,
        HARDENED
    }
    
}